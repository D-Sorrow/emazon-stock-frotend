export enum inputType {
    TEXT = 'text',
    NUMBER = 'number',
    CHECKBOX = 'checkbox',
    RADIO = 'radio',
    SELECT = 'select',
    MULTIPLE_SELECT = 'multiple-select',
    TEXTAREA = 'textarea',
    DATE = 'date',
    FILE = 'file',
    CUSTOM = 'custom',
}